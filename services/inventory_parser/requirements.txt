flask
flask-cors
gunicorn
opencv-python-headless
numpy
scipy
torch
# transformers provides CLIPModel for icon matching. (Florence-2 removed, so the
# old <5.0 pin it needed is gone — CLIP works on 4.x and 5.x alike.)
transformers>=4.38.0
Pillow
protobuf
# Gemini model chain is the sole quantity OCR path (3.1-flash-lite primary, with
# 2.5-flash / 3.5-flash / 2.5-flash-lite as per-model-quota fallbacks). On total
# chain exhaustion the parser returns quantity=0 for manual entry.
google-genai
python-dotenv
